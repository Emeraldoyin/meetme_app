import 'package:flutter/material.dart';

import 'user.dart';

class ResultScreen extends StatelessWidget {
  final User user;

  const ResultScreen({Key? key, required this.user}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: ... display user information, fun fact generator, and buttons
    throw UnimplementedError();
  }
}
